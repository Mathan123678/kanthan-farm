services:
  - type: web
    name: kanthan-farm-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node src/index.js
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 4000
    healthCheckPath: /api/health
